<nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #D42424">
  <div class="container">
    <a class="navbar-brand" href="/">Prism2.0</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapsedContent" aria-controls="navbarCollapsedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarCollapsedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <a class="nav-link text-light" href="/"> Main </a>
        </li>
        <% if logged_in? %>
        <li class="nav-item">
          <a class="nav-link text-light" href="/maps"> Maps </a>
        </li>
        <% end %>
      </ul>
      <% if !logged_in? %>
        <span class="mr-2"><%= link_to "Log in", login_path, class: "btn btn-light" %></span>
        <span class=""><%= link_to "Sign up", signup_path, class: "btn btn-light" %></span>
      <% else %>
        <span class="dropdown">
          <a href="#" class="dropdown-toggle mr-2" data-toggle="dropdown"> Account <b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li><%= link_to "Profile", current_user %></li>
            <li><%= link_to "Settings", '#' %></li>
            <li class="divider"></li>
          </ul>
        </span>
        <%= link_to "Log out", logout_path, method: :delete %>
      <% end %>
    </div>
  </div>
</nav>